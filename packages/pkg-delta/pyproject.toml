[project]
name = "pkg-delta"
version = "0.1.0"
description = "Dummy package delta - depends on alpha (sibling of beta)"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "pkg-alpha>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pkg_delta"]
