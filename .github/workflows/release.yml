name: Release Wheels
on:
  workflow_dispatch:
    inputs:
      plan:
        description: Release plan JSON (generated by uvr release).
        type: string
        required: true
permissions:
  contents: write
jobs:
  pre-build:
    runs-on: ubuntu-latest
    if: ${{ !contains(fromJSON(inputs.plan).skip, 'pre-build') }}
    steps:
    - name: Never
      run: echo 'Never'
  build:
    runs-on: ${{ matrix.runner }}
    if: ${{ !contains(fromJSON(inputs.plan).skip, 'build') }}
    needs:
    - pre-build
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - uses: astral-sh/setup-uv@v5
    - name: Set up Python
      run: "uv python install ${{ fromJSON(inputs.plan).python_version }}\nuv tool install ${{ fromJSON(inputs.plan).uvr_version != '' && format('uv-release-monorepo=={0}', fromJSON(inputs.plan).uvr_version) || 'uv-release-monorepo' }}"
    - name: Build packages
      env:
        GH_TOKEN: ${{ github.token }}
        UVR_PLAN: ${{ inputs.plan }}
      run: uvr-steps build-all --plan "$UVR_PLAN" --runner '${{ matrix.runner }}'
    - uses: actions/upload-artifact@v4
      with:
        name: wheels-${{ matrix.runner }}
        path: dist/*.whl
        if-no-files-found: error
    strategy:
      fail-fast: false
      matrix:
        runner: ${{ fromJSON(inputs.plan).runners }}
  post-build:
    runs-on: ubuntu-latest
    if: ${{ always() && !failure() && !contains(fromJSON(inputs.plan).skip, 'post-build') }}
    needs:
    - build
    steps:
    - name: Never
      run: echo 'Never'
  pre-release:
    runs-on: ubuntu-latest
    if: ${{ always() && !failure() && !contains(fromJSON(inputs.plan).skip, 'pre-release') }}
    needs:
    - post-build
    steps:
    - name: Never
      run: echo 'Never'
  publish:
    runs-on: ubuntu-latest
    if: ${{ always() && !failure() && !contains(fromJSON(inputs.plan).skip, 'publish') }}
    needs:
    - pre-release
    steps:
    - uses: actions/download-artifact@v4
      with:
        pattern: wheels-*
        merge-multiple: true
        path: dist
        run-id: ${{ fromJSON(inputs.plan).reuse_run_id != '' && fromJSON(inputs.plan).reuse_run_id || github.run_id }}
    - uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ matrix.tag }}
        name: ${{ matrix.title }}
        body: ${{ matrix.body }}
        files: ${{ matrix.dist_name }}-${{ matrix.version }}*.whl
        make_latest: ${{ matrix.make_latest }}
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJSON(inputs.plan).publish_matrix }}
  finalize:
    runs-on: ubuntu-latest
    if: ${{ always() && !failure() && !contains(fromJSON(inputs.plan).skip, 'finalize') }}
    needs:
    - publish
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - uses: astral-sh/setup-uv@v5
    - name: Set up Python
      run: "uv python install ${{ fromJSON(inputs.plan).python_version }}\nuv tool install ${{ fromJSON(inputs.plan).uvr_version != '' && format('uv-release-monorepo=={0}', fromJSON(inputs.plan).uvr_version) || 'uv-release-monorepo' }}"
    - name: Finalize release
      env:
        UVR_PLAN: ${{ inputs.plan }}
      run: uvr-steps finalize --plan "$UVR_PLAN"
  post-release:
    runs-on: ubuntu-latest
    if: ${{ always() && !failure() && !contains(fromJSON(inputs.plan).skip, 'post-release') }}
    needs:
    - finalize
    steps:
    - name: Never
      run: echo 'Never'
